/**
 * Contains Duplicate - Stub Implementation
 *
 * This is a stub implementation to satisfy TypeScript's type checking
 * while following TDD principles. The actual implementation will be
 * added after confirming test failures (RED phase).
 *
 * @internal
 */
export function containsDuplicate(nums: number[]): boolean {
  // スタブ実装: 引数は使用するが、まだ実装していない / Stub implementation: using parameter but not yet implemented
  void nums;
  // TDD: REDフェーズを確実にするため、常に例外を投げる / TDD: Always throw to ensure RED phase
  throw new Error('Not implemented yet');
}
